# Workspace libraries are bundled into the desktop app by electron-vite, so the
# default isolated node_modules layout is fine. Native addons that ship later
# (for example onnxruntime-node) are externalized and unpacked from asar by
# electron-builder rather than hoisted here.
auto-install-peers=true
strict-peer-dependencies=false
